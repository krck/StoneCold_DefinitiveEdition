{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "BUILD with Make",
			"detail": "Run the Makefile script via mingw and create the .dlls and .exe based on Make",
			"type": "shell",
			"command": "mingw32-make",
			"problemMatcher": [
				"$gcc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}